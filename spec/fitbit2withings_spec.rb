require 'spec_helper'

describe Fitbit2withings do
  it 'has a version number' do
    expect(Fitbit2withings::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
